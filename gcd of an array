#include<bits/stdc++.h>
using namespace std ; 
int calculate_gcd(vector<int>g ,int  a)
{
  int result = g[0];
  for (int i = 0; i < a; ++i)
  {
    result=__gcd(g[i],result);

  }
  cout<<"the output is "<<endl;
  cout<<result<<endl;
return result ; 
}
int main()
{
  #ifndef ONLINE_JUDGE
freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);
#endif 
  vector<int>g{2,4,6,8,16} ; 
  int b = g.size();
  calculate_gcd(g,b);

}
